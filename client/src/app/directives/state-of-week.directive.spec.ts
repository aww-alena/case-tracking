import { StateOfWeekDirective } from './state-of-week.directive';

describe('StateOfWeekDirective', () => {
  it('should create an instance', () => {
    const directive = new StateOfWeekDirective();
    expect(directive).toBeTruthy();
  });
});
